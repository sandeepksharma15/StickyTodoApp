<Window x:Class="StickyTodoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="StickyTodo"
        Width="420"
        MinWidth="300" MinHeight="260"
        WindowStyle="None"
        ResizeMode="CanResize"
        SizeToContent="Height"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="False">

    <Border Background="{Binding CurrentThemeKey, Converter={StaticResource ResourceKeyToBrushConverter}}"
            BorderBrush="{StaticResource NoteBorder}"
            BorderThickness="2"
            CornerRadius="16"
            Padding="12">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.35" ShadowDepth="4" Color="Black"/>
        </Border.Effect>

        <!-- Use DockPanel so window remeasures when scrollbars appear/disappear -->
        <DockPanel LastChildFill="True">
            <!-- Right theme buttons panel -->
            <StackPanel DockPanel.Dock="Right" Margin="8,0,0,8" VerticalAlignment="Bottom">
                <ItemsControl ItemsSource="{Binding AvailableThemeKeys}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Width="26" Height="26" Margin="0,0,0,8" Command="{Binding DataContext.ChangeThemeCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <Border CornerRadius="13" Width="26" Height="26">
                                            <Border.Background>
                                                <SolidColorBrush Color="{Binding Converter={StaticResource ThemeKeyToColorConverter}}"/>
                                            </Border.Background>
                                        </Border>
                                    </ControlTemplate>
                                </Button.Template>
                            </Button>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

            <!-- Main content grid -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Sticky TODO" FontSize="20" FontWeight="Bold" Foreground="Black" HorizontalAlignment="Center" Margin="0,0,0,8"/>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBox Width="140" Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" KeyDown="TextBox_KeyDown"/>
                    <ComboBox Width="70" Margin="6,0,0,0" ItemsSource="{Binding PriorityValues}" SelectedItem="{Binding NewPriority}" VerticalAlignment="Center"/>
                    <DatePicker Width="130" Margin="6,0,0,0" SelectedDate="{Binding NewDueDate}" VerticalAlignment="Center"/>
                    <Button Content="Add" Margin="6,0,0,0" Command="{Binding AddCommand}" VerticalAlignment="Center"/>
                </StackPanel>

                <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" MaxHeight="300">
                    <ItemsControl ItemsSource="{Binding OpenItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,4">
                                    <CheckBox IsChecked="{Binding IsDone, Mode=TwoWay}" Margin="0,0,6,0"/>
                                    <TextBox Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="140" Margin="0,0,6,0"/>
                                    <ComboBox Width="70" ItemsSource="{Binding DataContext.PriorityValues, RelativeSource={RelativeSource AncestorType=Window}}" SelectedItem="{Binding Priority, Mode=TwoWay}" Margin="0,0,6,0"/>
                                    <DatePicker SelectedDate="{Binding DueDate, Mode=TwoWay}" Width="130"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Separator Grid.Row="3" Margin="0,8,0,8"/>

                <ScrollViewer Grid.Row="4" VerticalScrollBarVisibility="Auto" MaxHeight="300">
                    <ItemsControl ItemsSource="{Binding DoneItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="0,4">
                                    <CheckBox IsChecked="{Binding IsDone, Mode=TwoWay}" Margin="0,0,6,0"/>
                                    <TextBlock Text="{Binding Title}" Foreground="Gray" TextDecorations="Strikethrough"/>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Border Grid.Row="5" BorderThickness="1" BorderBrush="#DDD" CornerRadius="6" Padding="6" Visibility="{Binding ShowArchive, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,8,0,8">
                    <StackPanel>
                        <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="150">
                            <ItemsControl ItemsSource="{Binding ArchivedItems}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="0,4">
                                            <TextBlock Text="{Binding Title}" Foreground="Gray"/>
                                            <TextBlock Text="{Binding CompletedOn, StringFormat=' (done {0:dd-MMM})'}" Foreground="DarkGray" Margin="4,0,0,0"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                        <TextBlock Text="No archived items" Foreground="DarkGray" FontStyle="Italic" HorizontalAlignment="Center" Margin="4">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding ShowArchive}" Value="True"/>
                                                <Condition Binding="{Binding ArchivedItems.Count}" Value="0"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,6">
                    <ToggleButton Content="{Binding ShowArchive, Converter={StaticResource BoolToArchiveTextConverter}}" IsChecked="{Binding ShowArchive}" Margin="0,0,8,0"/>
                    <Button Content="Clear Archived" Command="{Binding ClearArchiveCommand}" ToolTip="Remove all archived items permanently" IsEnabled="{Binding ArchivedItems.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Enable}"/>
                </StackPanel>
            </Grid>
        </DockPanel>
    </Border>
</Window>
