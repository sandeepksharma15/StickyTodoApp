<Window x:Class="StickyTodoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="StickyTodo"
        Width="300" Height="400"
        MinWidth="200" MinHeight="200"
        WindowStyle="None"
        ResizeMode="CanResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        Topmost="False">

    <Border Background="{StaticResource NoteGreen}"
            BorderBrush="{StaticResource NoteBorder}"
            BorderThickness="2"
            CornerRadius="16"
            Padding="12">

        <Border.Effect>
            <DropShadowEffect BlurRadius="20"
                              Opacity="0.35"
                              ShadowDepth="4"
                              Color="Black"/>
        </Border.Effect>

        <StackPanel>
            <TextBlock Text="Sticky TODO"
               FontSize="20"
               FontWeight="Bold"
               Foreground="Black"
               HorizontalAlignment="Center"
               Margin="0,0,0,8"/>

            <!-- Open Items -->
            <ItemsControl ItemsSource="{Binding OpenItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <CheckBox IsChecked="{Binding IsDone, Mode=TwoWay}"
                              Margin="0,0,6,0"/>
                            <TextBlock Text="{Binding Title}"
                               FontWeight="{Binding Priority, Converter={StaticResource PriorityToFontWeightConverter}}"
                               Foreground="Black"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Separator Margin="0,8,0,8"/>

            <!-- Done Items -->
            <ItemsControl ItemsSource="{Binding DoneItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,4">
                            <CheckBox IsChecked="{Binding IsDone, Mode=TwoWay}"
                              Margin="0,0,6,0"/>
                            <TextBlock Text="{Binding Title}"
                               Foreground="Gray"
                               TextDecorations="Strikethrough"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </Border>
</Window>
